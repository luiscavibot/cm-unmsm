'use strict';

/**
 * categoria-noticia service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::categoria-noticia.categoria-noticia');
