'use strict';

/**
 * sub-categoria-noticia service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sub-categoria-noticia.sub-categoria-noticia');
