'use strict';

/**
 * sub-categoria-noticia router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sub-categoria-noticia.sub-categoria-noticia');
