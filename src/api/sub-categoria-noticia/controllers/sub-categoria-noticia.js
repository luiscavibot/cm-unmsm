'use strict';

/**
 *  sub-categoria-noticia controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::sub-categoria-noticia.sub-categoria-noticia');
